spring:
  config:
    activate:
      on-profile: adapter-data-jpa-local
  datasource:
    master:
      driverClassName: com.mysql.cj.jdbc.Driver
      jdbcUrl: jdbc:mysql://localhost:3306/dudgns
      username: dudgns
      password: dudgns0258!
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
  flyway:
    enabled: false
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace